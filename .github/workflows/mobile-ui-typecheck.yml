name: mobile-ui-typecheck

on:
  push:
    paths:
      - "apps/mobile/**"
      - "packages/ui/**"
      - "packages/game-client/**"
      - "packages/game-engine/**"
      - "package.json"
      - "pnpm-workspace.yaml"
  pull_request:
    paths:
      - "apps/mobile/**"
      - "packages/ui/**"
      - "packages/game-client/**"
      - "packages/game-engine/**"
      - "package.json"
      - "pnpm-workspace.yaml"

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: corepack enable
      - run: pnpm install --no-frozen-lockfile
      - run: pnpm --filter @sakkah-baloot/ui typecheck
      - run: pnpm --filter @sakkah-baloot/game-client typecheck
      - run: pnpm --filter @sakkah-baloot/mobile typecheck
