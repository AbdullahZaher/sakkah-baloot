{
  "name": "@sakkah-baloot/game-simulator",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic full-match simulation boundary for AI validation",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "pnpm --filter @sakkah-baloot/game-engine build && pnpm --filter @sakkah-baloot/game-protocol build && pnpm --filter @sakkah-baloot/game-ai build && npm run build && tsx --test test/*.test.mjs"
  },
  "dependencies": {
    "@sakkah-baloot/game-engine": "workspace:*",
    "@sakkah-baloot/game-ai": "workspace:*"
  },

  "devDependencies": {
    "typescript": "^5.0.0",
    "tsx": "^4.20.6"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  }
}