{
  "name": "@sakkah-baloot/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "npm run build && node --import tsx --test test/*.test.mjs"
  },
  "dependencies": {
    "@sakkah-baloot/game-engine": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "^5.0.0"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts"
}